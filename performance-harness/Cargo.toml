[package]
name = "sdax-performance-harness"
version = "0.0.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
sdax = { path = "../crates/sdax" }
sdax-tokio = { path = "../crates/sdax-tokio" }
tokio = { version = "=1.53.1", default-features = false, features = ["rt", "sync", "time"] }

[profile.release]
codegen-units = 1
lto = "thin"

