Write a complete Rust candidate.rs using only sdax, sdax-tokio, tokio and std. It is compiled as a module alongside the supplied immutable support module. Import crate::support::* and sdax::prelude::* as needed. Supply pub fn build(env: Env) -> Plan<u32,u32> and pub fn boundary(report: Report<u32>) -> Result<u32,String>. build describes the graph without executing external work; the evaluator starts it. Keep Report internally and implement the required string boundary through into_result and full report formatting; success dereferences the exported Arc<u32>, missing output is an error. Use only engine-owned work, declarative retries/restarts and single-use acquisition. Resource-bearing component imports must be formal ports; don't hide a resource handle in ordinary typed data or closures. Use Env.open inside cx.hold, and Env.close for resource releases. Pass all immediate external resource parents to Env.open. Env records actual open/close calls, checks live-parent/child order, and supports injected cleanup failure. Explicit extra Env.record events below are observations at the described operations, never precomputed outcomes. No main or tests. Return only Rust source.

New held-out case: contrasting acknowledged and unresolved receipts in repeated component mounts. A reusable child takes operation identity u32, with one identified effect named dispatch. Identity even: external operation returns real receipt identity+900 and records ack:<identity>; compensation must record undo:<receipt>. Identity odd: record unknown:<identity>, remain pending without receipt until a 3ms deadline, recovery records reconcile:<identity> and returns Recovery::StillUnknown. Both have on_ambiguous Recover and idempotent safety. Mount same definition as delivered with input 2*n, and pending with input 2*n+1 under Policy::Isolate. Every child exports independent completed value 1. Root exports n. The unresolved report includes pending/dispatch and ambiguity; no fabricated receipt compensation for the odd identity and no reconciliation for the even one. Run twice with different n. boundary must contain the full pending dispatch path and unresolved diagnostic.
