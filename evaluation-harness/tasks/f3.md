Write a complete Rust candidate.rs using only sdax, sdax-tokio, tokio and std. It is compiled as a module alongside the supplied immutable support module. Import crate::support::* and sdax::prelude::* as needed. Supply pub fn build(env: Env) -> Plan<u32,u32> and pub fn boundary(report: Report<u32>) -> Result<u32,String>. build describes the graph without executing external work; the evaluator starts it. Keep Report internally and implement the required string boundary through into_result and full report formatting; success dereferences the exported Arc<u32>, missing output is an error. Use only engine-owned work, declarative retries/restarts and single-use acquisition. Resource-bearing component imports must be formal ports; don't hide a resource handle in ordinary typed data or closures. Use Env.open inside cx.hold, and Env.close for resource releases. Pass all immediate external resource parents to Env.open. Env records actual open/close calls, checks live-parent/child order, and supports injected cleanup failure. Explicit extra Env.record events below are observations at the described operations, never precomputed outcomes. No main or tests. Return only Rust source.

New held-out case: retry followed by an unknown operation outcome. Root input n opens permit value n. Build a per-run identity u32 n*100+17 as a step value, declared before effect send, and use permit as an ordinary dependency. send has exactly three total idempotent attempts, fixed 1ms backoff, 4ms body deadline, and unknown-outcome recovery. In its lazy external operation record send:<attempt>:<identity>:<permit.value>:<identity-Arc-pointer> using Arc::as_ptr(&identity) as usize. Attempts 1 and 2 return known errors; attempt 3 remains pending without receipt. Recovery records recover:<attempt>:<identity>, returns Recovery::Resolved, and MUST run while permit remains live. To exercise this check recovery calls env.open("audit", identity, &[]) and closes audit before permit release. Use env.record("permit-live-at-recovery") only after checking env.live_count()==1 before opening audit; otherwise return an error. Compensation records compensate:<receipt> and must not run. Export an independent completed n+1 data step. The timeout report remains failure, with no unresolved ambiguity after successful recovery, and boundary must preserve send/prepare/timeout. All attempts use the same actual identity Arc, not recomputed IDs.
