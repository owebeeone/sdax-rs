# Stage 0 — TDD log

LBT-007: every behaviour change begins with a failing test. One line per step:
the test written, the RED evidence observed, and the change that made it GREEN.
RED lines quote what the compiler or the runner actually said.

Convention: a test that does not compile *is* a failing test — for a typed
surface it is usually the only failure available before the type exists.

| # | test written first | RED | GREEN |
|---|---|---|---|
| 1 | `tests/keys.rs`: keys are `Copy + Send + Sync` for sized and unsized `T`; `Deps` maps tuples to `Arc` tuples to 8; `raw_keys` in written order; a `Slots` miss is `None`, never a wrong downcast | `error[E0425]: cannot find type 'Key' in this scope` (×4), `error[E0405]: cannot find trait 'Deps'` | `src/key.rs`: `RawKey`, `Key<T>`, `Deps` (`()`, `Key<A>`, tuples 1–8 via a private `macro_rules!`), `Slots`. 4 passed |
| 2 | `tests/seam.rs`: `hold` registers in the completing poll and not while pending; a failed effect registers nothing; `hold_value` registers before returning; unsized outputs; `stop`/`until_stop`/`is_stopping`; `now`/`sleep`/`timeout` on an injected clock; `attempt`; `Serving::new`; `spawn` with no run attached | 53 errors, `cannot find type 'Cx'`, `'CxInner'`, `'Held'`, `'Time'`, `cannot find trait 'Clock'` | `src/contracts.rs` (`Time`, `Clock`, `Error`, `BoxFuture`) and `src/cx.rs` (phases, `CxInner`, `Cx`, `StopSignal`/`Stop`, `Hold`, `Held`, `Serving`, `Child`, `Scope`, `SpawnError`). One follow-up RED: `error[E0277]: 'dyn Future<Output = ()>' cannot be unpinned` from the test helper — `poll_once` made generic. 15 passed |
| 3 | `tests/builder.rs` over `tests/corpus.rs` (I-01, I-05, I-07, I-15, I-16, I-18, I-23, I-24, I-27, I-29, I-30, I-32): the recorded declaration is exactly what was written; `release::by_drop()` records `drop`; `.persistent()` records `persistent`; attribute order is free; a component carries its child plan; a template records its imports and its input; `spawns` is recorded | 102 errors, `unresolved import 'crate::plan'`, `cannot find type 'Plan'`, `'Shutdown'` | `src/policy.rs`, `src/plan.rs`, `src/builder.rs`, `src/terminals.rs`; `validate()` still an empty stub, which the next step turns red. Follow-up RED: the attribute-order test compared two plan identities — the test normalises them. 23 passed |
| 4 | `tests/planner_validate.rs`: P-01, P-02, P-05 (+ component variant), P-06 (retry, ambiguity, restart), P-07 (+ two-level nesting), P-08, P-09 (+ variants), P-10, P-11, P-12, `V-MODE` (both directions), `V-SPAWN-SELF-IMPORT`, the finding shape, `L-IMPORTS` | 57 errors, `no variant 'ForeignKey' found for enum 'validate::Rule'`, `no field 'nodes' on type '&validate::Finding'` | `src/validate.rs`: `Rule` (14 rules with `id()` and an emission order), `Finding`, `Invalid`, and one function per rule. Follow-up RED: `p09_a_template_holder_counts_as_unbounded` was unwritable through the surface (a child plan naming a parent pool is `V-FOREIGN-KEY` first) — replaced by the reachable case plus a white-box test over a hand-built IR. 43 passed |
| 5 | `tests/planner_view.rs`: P-03 (nodes, exactly the declared edges, layers, release order, `why`, mode, rendering), P-04 (flat, nested, dynamic), `spawns` visible, `effects()` incl. a persistent effect and a plan that ships nothing, P-13 (empty diff, authored change, simulated default change), P-14, `why` for a lock and for a pool | 26 errors, `unresolved import 'crate::view::Reason'`, `cannot find 'PlanView' in 'view'` | `src/view.rs` + `view/model.rs`, `view/render.rs`, `view/diff.rs`. Follow-up RED: `default_changed` was false for an authored `cooperative` — `Attrs.set_twice` became `Attrs.declared` (which attributes the author set), which also simplified `V-DUP-ATTR`. Then two rendering defects found by eye: a template's import resolved to `0`, and `stop_within` printed on resources. 51 passed |
| 6 | `tests/shorthand.rs`: the positional form of I-01 produces the same `PlanView` and the same rendering as the chain form; every body-carrying kind has one; the two forms mix in one plan | 13 errors, `no method named 'resource_with' found for struct 'PlanBuilder'` | `src/shorthand.rs`: seven positional constructors, each one line delegating to the chain. 54 passed |
| 7 | `tests/report_order.rs`: records order by declaration, then instance, then attempt; `Report::sort` orders every list and leaves the trace alone; `is_clean` is false for an abandoned or ambiguous node; panics are carried; the engine's `Event`/`Effect` names exist; the host contracts are implementable and `dyn`-usable | 62 errors, `cannot find type 'RecordOrder'`, `'Fault'`, `'Report'`, `use of unresolved module 'crate::engine'` | `src/report.rs` and `src/engine.rs`; `Runtime`/`TaskHandle`/`Joined`/`Observer`/`NoObserver` restored to `src/contracts.rs`. Follow-up RED: the ordering test built `"Link#1/Entry"` as one path segment — fixed in the test. 60 passed |
| 8 | `src/compile_fail.rs`: 14 `compile_fail` doctests, each carrying the error code it claims (W-01…W-17) | n/a — the failure a `compile_fail` test asserts *is* the compiler's; the RED direction is the opposite one, so `scripts/compile-fail.sh` was written to assert **why** each fails | 14 doctests pass; `scripts/compile-fail.sh` reports all 14 rejected with the claimed code (`E0425`, `E0271`, `E0451`, `E0599`, `E0631`, `E0308`, `E0425`, `E0599`, `E0599`, `E0061`, `E0308`, `E0277`, `E0308`, `E0451`) |
| 9 | `crates/sdax/tests/witness.rs`: the crate seen from outside — W-14 (typed deps, `Arc<dyn Trait>`, `Send + Sync`, free attribute order, `?` inference), A6 (findings as values), A9 (registration in the completing poll), W-15 (a start body that moves `cx` into the serve future), a bounded cooperative release, and that Stage 0 has no execution | test file did not compile: `Kind` was not exported | `pub use plan::{Kind, ReleaseStyle, ...}`. 6 passed |
| 10 | `crates/sdax-testkit/tests/testkit.rs`: the fake clock satisfies the clock contract and never advances by itself; the recorder keeps observation order; the static checker accepts a well-formed plan, catches an edge nobody declared, and agrees with the view's unordered pairs | `error[E0432]: unresolved imports 'sdax_testkit::invariants', 'sdax_testkit::FakeClock', 'sdax_testkit::TraceRecorder'` | `sdax-testkit`: `FakeClock`, `TraceRecorder`, `invariants::{check_plan, check_clock, poll_once}`. Follow-up RED: the INV-5 negative fixture reported INV-1 instead — INV-5/INV-6 have no negative fixture through the public API, which is now stated in the module docs and covered positively across shapes. 7 passed |
| 11 | `crates/sdax-tokio/tests/adapter.rs`: `TokioRuntime` satisfies `Runtime` (compile witness through a generic bound); a task runs and joins `Done`; an abort joins `Cancelled` and the body never resumed; a panic joins `Panicked` and does not escape; a blocking body runs off the caller's thread; the clock moves only when tokio time moves; an observer can be installed; every task is tracked; a shutdown that runs out of budget says what is still running | `error[E0432]: unresolved import 'sdax_tokio::TokioRuntime'` | `sdax-tokio`: `TokioRuntime`, `TokioTask`, `TokioClock`, `FnObserver`, tokio 1.53.1 + tokio-util 0.7.19 pinned with minimal features. Follow-up RED: `the_tokio_clock_only_moves_when_tokio_time_moves` failed with `left: 4999992750, right: 5000000000` — a process-global clock origin was shared across two paused runtimes; the origin is now per clock. 9 passed |
| 12 | `scripts/check-architecture.sh` — its own negative fixtures, run by hand: removing `publish = false` from the harness, and giving the pure core a `tokio` dependency | `ARCHITECTURE GATE FAILED — LBT-005: sdax-testkit must set publish = false` (exit 1); `LBT-003: the pure core must have zero normal dependencies, has ['tokio']` (exit 1) | both reverted; gate passes with exit 0. The fixtures are not committed as tests: they mutate manifests |

## Refactors after GREEN (behaviour unchanged, tests re-run)

| change | why | result |
|---|---|---|
| `cargo fmt` over the whole tree | the repo takes rustfmt's defaults, with no `rustfmt.toml` | `cargo fmt --check` clean |
| `validate.rs` → `validate.rs` + `validate/rules.rs` | 691 lines, over the ~500 budget | 200 + 502 |
| `view.rs` → `view.rs` + `view/model.rs` | 651 lines | 501 + 155 |
| `cx.rs` → `cx.rs` + `cx/instances.rs` | 603 lines | 434 + 186 |
| clippy `-D warnings` cleanup | the gate | 0 warnings across the workspace |
