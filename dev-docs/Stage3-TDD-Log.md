# Stage 3 — TDD log

LBT-007: every behaviour change begins with a failing test. One line per step:
the test written, the RED evidence observed, and the change that made it GREEN.
RED lines quote what the compiler or the runner actually said.

Convention: a test that does not compile *is* a failing test. The Stage 3 test
sources are `sdax-v1/B/CanonicalTests.md` § 4 rows `C-30`, `C-51`, `C-64`,
`C-65` and the INV-16 half of `C-14`, plus `corpus/Intents.md` I-30 and I-34.

Resume point: read the last row of the table below; the workspace is green at
every row unless the row says otherwise.

| # | test written first | RED | GREEN |
|---|---|---|---|
| 1 | `crates/sdax/src/tests/instances.rs` (new): the `HC` mesh — an `Endpoint` resource, a `Link` template importing it, an `Acceptor` service declaring `spawns(&link)` — with five tests: `Machine::new` accepts it, the template node comes up `Live`, a spawn opens an instance and admits its nodes, and `spawn_check` answers the three `SpawnError`s | `error[E0599]: no variant … named 'Live' found for enum 'NodeState'`; `error[E0559]: variant 'Event::InstanceSpawned' has no field named 'spawner'`; `no method named 'instance_nodes'`; `no method named 'spawn_check'` (4×) | the machine's instance half. `host/engine/table.rs`: `Node` gains `decl`, `instance`, `child`, `spawns`; `Scope` gains `instance` and a per-instance key `map`; `Table::instantiate` appends one instance's scope and nodes at run time with **re-keyed** `RawKey`s (one fresh plan identity per instance scope) so every node of a run has a unique key an `Event` can name; `Kind::Input` is dropped like `Kind::Import`, so a need on the per-instance input is satisfied from the spawn. `host/engine/instances.rs` (new): `spawn_check`, `SpawnTable`, `instances`, `instance_nodes`, `origin`, `on_instance_spawned`, `on_stop_instance`, the template's stop obligation and `end_instance`. `Machine::new` no longer refuses templates — `EngineError::Templates` is replaced by `EngineError::TemplateAsScope`, which refuses a *template plan* used as a root or a component. `Event::InstanceSpawned` gains `spawner`; `Event::InstanceEnded` becomes `Event::StopInstance` (`OD-INSTANCE-EVENTS`); `Effect::SpawnInstance` gains `parent`. `NodeState::Live` and `St::{Live, StoppingInstances}` are new |
| 2 | same file: `a_live_instance_holds_the_key_it_imports_until_it_ends` (INV-16 / `C-14`), `one_instance_can_be_closed_while_the_other_keeps_running` (`C-30`'s shape), `instance_records_carry_the_instance_in_their_order` (INV-20 / F4) | `the endpoint releases once the instance has: [Emit(… ReleaseOk), CancelTimer(3), Emit(… InstanceEnded(#1, Ok)), Emit(… Stopped)]` — the test's own ordering assumption: the `Acceptor` service is the endpoint's *other* dependent and had not stopped yet | the machine was right and the assertions were rewritten to say so, which is the stronger statement: the endpoint releases when **every** dependent has finished, the live instance among them. `cleanup.rs`: `in_flight` counts a template's live instances the way it counts a component's inner scope; `owes` gives a `Live` template the "stop every live instance" obligation; `blocks_release` knows `StoppingInstances`; `try_cleanup` opens an instance's release graph with no gate in the parent (INV-16 forbids a parent node to name an instance node, so an instance is only ever a dependent); `check_end` ends an instance rather than the root; `abandon_all` abandons a template's instances in reverse declaration order. `settle.rs` gains `is_root` (a scope that is neither a component's nor an instance's) and promotes a `Pending`/`Waiting` template with live instances to `Live`. **80 passed, 0 failed, 1 ignored** in suite (b); the workspace is green at 288 |
| 3 | `crates/sdax/tests/surface.rs` and the whole workspace against a `BodySource` that carries the instance: `src.body(key, None, &inner)` | `error[E0050]: method 'body' has 3 parameters but the declaration in trait has 4`; `error[E0046]: not all trait items implemented, missing: 'open_instance', 'close_instance'` | the host-surface change Stage 2 § 11 predicted. `BodySource` gains `instance: Option<InstanceId>` on `body`, `cleanup` and `store`, and the two new methods; `PlanBodies` keeps one `Vec<ScopeBodies>` **per instance**, and `find(plan, instance)` resolves a plan's slots from the instance outwards — that instance's, then the instance that spawned it, then the static graph — so a nested template reads the right copy. `PlanBuilder::template` now records the template plan's bodies in `Bodies::children` (it recorded only the IR, so a template's bodies were unreachable), and `child_bodies` walks components and templates together. `CxInner::spawn_instance` is new host API: `Cx::spawn` is the typed form, and a harness with erased bodies has only the key. `Cx::spawn<I>` and `Scope::spawn_instance` tightened to `Send + Sync` (`OD-SPAWN-INPUT`): the input lives in the instance's slot table, and `Plan::template::<In>` already required it, so no `Template` that can be built is excluded |
| 4 | `crates/sdax-testkit/tests/conformance/instances.rs` (new, included by **both** binaries): `C-30`, `C-51`, `C-64`, `C-65`, `C-14`'s INV-16 half and `I-34` | `no method named 'spawned' found for struct 'Eol'` (and `instance_ends`, `instance_end_at`, `at_of`); then, once those existed, **`INV-12: Link/Sock attempt 1 started where attempt 2 was expected` / `INV-12: … while attempt 1 was in flight` / `INV-3: … was released 2 times`** on `C-30` and `I-34` | the checker was grouping two instances into one node. `invariants/occ.rs` (new): an `Occ` is the instance chain a `RecordOrder` already carries, one entry per path segment, and `(path, occ)` is the identity of a *copy*; `occ_of_target` says which copy of a need, lock or template a copy names, and `occ_of_scope` which copy of a scope a pool belongs to. `trace.rs`, the new `report.rs` (split out of it) and `arbitration.rs` all group by it. Script side: `SpawnSpec` (`spawn T @t`, `.awaited()`, `.stopped(@t)`) on `Script`; `sim/instances.rs` + `sim/effects.rs` (the simulator split) perform the directives through `Machine::spawn_check`, hold a start body's return until its awaited instances answer (INV-17) and give each new instance's nodes a script by path; `ScriptedBodies` does the same with real `cx.spawn` calls, a real `child.ready().await` and a stopper future beside the serve. `Driven`/`Recorded` carry `spawns`, so a row can assert the `SpawnError` a body was handed. The tokio side: `sdax-tokio/src/scope.rs` (new) is the run's `Scope` and its `Child`ren's `ChildControl`, deciding against the `SpawnTable` the driver publishes after every step; the driver attaches the scope to every body context, performs `SpawnInstance`, and drops an instance's slots when the trace says it ended. **6 rows green on both drivers**; suite (c) is 74 on the scripted driver and 78 on the adapter |
| 5 | no new test — the rule audit AGENTS.md asks for: every checker rule read for "does this skip an instance?" | `MUTEX` compared *paths*, so two instances of one template contending for a key they both import were never compared at all, and `POOL` counted one pool where a template's plan declares one per instance | `arbitration.rs` rewritten over copies: `grant_spans` takes an `Occ`, `MUTEX` pairs copies (two copies of one path included) and only when `occ_of_target` says they name the **same** copy of the resource, `POOL` groups users by `occ_of_scope`, and `SKIPPED`, `TERMINAL` and `T5-INNER` all resolve the copy they mean. `containment.rs` (new) adds INV-16's two declaration clauses, the instance lifecycle (`INSTANCE`: spawned once, ended once, never observed outside that window) and `T5-INSTANCE`. **302 passed, 0 failed, 2 ignored** |
| 6 | `crates/sdax-testkit/src/mc/templates.rs` (new) and `mc/script.rs`: the walk declares templates — importing up to two of the enclosing scope's keys, taking a `u8` per instance with a node that reads it, nested up to two deep — and generates `cx.spawn` directives for the services that declare them: several per run, at random ticks, some awaited, some closed again, some naming a template of another plan, some scheduled after the body returns or after the scope settles. Two new deliberate mistakes, `V-SPAWN-KIND` and `V-SPAWN-SELF-IMPORT`. Fourteen new coverage floors | **case seed 10566988825915931949**: `INV-16: instance #1 of Tpl was still live at End`. Then **seed 17176503344268963149**: `INV-5: C2's cleanup started at #17 while dependent Tpl had not finished`. Then **seed 1732908402085695841**: `INV-16: template C3/C3/Tpl spawned instances and its obligation never ended` | three machine fixes to one rule. `owes` for a template read `St::Live` alone, so a template an isolated fault had already `Skipped` stopped owing and the run ended with its instance still running; it now owes while it is live **and** while it ever spawned, whatever state the node reached, and `keep_template_live` (called from `settle`, `skip_scope` and `skip_dependents`) keeps a template that admitted an instance out of `Skipped` altogether. A template that reached `Live` now always closes its record with `StopRequested`/`Stopped`, so a reader can tell "done with its instances" from "still holding them". Generator fixes: `child_plan` reset the template nesting depth, so the walk built `Tpl/Tpl/Tpl/Tpl/Tpl/…`; `Driven::keys_of` answers for a node with one run key per instance and none at all when the run never instantiated it |
| 7 | `monte_carlo_big` (50 000 cases) and the adapter walk (20 000) | **seed 12339652235566683353**: `MUTEX: Tpl/Tpl/S2#1 and Tpl/Tpl/S2#2 hold the same resource at once`, and in the same trace a template with both an `Abandoned` and a `Stopped`. Then **seed 1520897640266743839**: `INV-16: instance #1 of Tpl was still live at End` with `INV-15: its release never ended`. Then **seed 6312987428304776340**: `stuck: the run stopped short of End`. Then, on the adapter only, **`SDAX_MC_SEED=6746427589533237250` index 281**: `stuck` again | four findings, three of them real. (1) A checker bug: `arbitration.rs` rebuilt a `NodePath` by splitting the rendered string on `/`, and a generated node's *name* contains one, so two instances' own copies of a lock looked like one shared lock — `path_of` now resolves against the view's own paths. (2) `owes` did not exclude `St::Abandoned`, so the budget's abandonment was followed by a second `StopRequested`. (3) `blocks_release` for a template ignored live instances once the template itself was abandoned, so the root ended while an instance was mid-cleanup; it now blocks while any instance is live, and the instance's zero timer (T7b) is what still terminates the run. (4) The `stuck` case was **not** a bug: a service with no `stop_within` inside *any* scope of an unbounded run has nothing to end its stop (T7a bounds an inner scope by the root's deadline until its release graph opens), which `V-SERVICE-UNBOUNDED` refuses for the root's own services and does not reach for a child's — a pre-existing gap the walk reached because templates multiplied inner scopes, recorded as Stage 3 open question 1 and steered around by `Shape::bounded_root`. The adapter-only `stuck` **was** a bug and is the one the pure walk cannot reach: `cx.spawn` passed the gate and the scope settled before the event arrived, and `settle` is a no-op on a `Planned` scope, so the instance's scope stayed `Planned` for ever. `on_instance_spawned` now skips and ends it at once, pinned by `a_spawn_that_lands_after_the_settle_ends_at_once` (RED: `left: [(InstanceId(7), Planned)]`). **200 000 pure cases and 20 000 adapter cases clean; every floor cleared** |
| 8 | `crates/sdax/tests/surface.rs`: `the_instance_hand_offs_are_reachable_from_another_crate` — `BodySource::{open_instance, close_instance}`, `Machine::{instances, instance_nodes, origin, instance_parent, spawn_check, spawn_table, declarations}`, `SpawnTable::check` and `CxInner::spawn_instance`, all named from a *consumer* crate | written after the change rather than before it: the signatures already existed, so this is the **pin**, not the driver, and it says so | the finishing pass. `RUSTDOCFLAGS="-D warnings"` found two broken intra-doc links; clippy found a complex type (`Found`), an `if` with identical blocks in `sim/effects.rs` and two `clone`s on a `Copy`. `tests/monte_carlo.rs` reached 788 lines and was split into `monte_carlo.rs` (419) and `monte_carlo/corners.rs` (384). All seven gates green: **302 passed, 0 failed, 2 ignored** |
