# Stage 2 — TDD log

LBT-007: every behaviour change begins with a failing test. One line per step:
the test written, the RED evidence observed, and the change that made it GREEN.
RED lines quote what the compiler or the runner actually said.

Convention: a test that does not compile *is* a failing test. The Stage 2 test
sources are `sdax-v1/B/CanonicalTests.md` § 5 (suite (d), `R-*`, `S-01`) and
§ 4's `C-14`, which the Stage 1 report deferred here.

Resume point: read the last row of the table below; the workspace is green at
every row unless the row says otherwise.

| # | test written first | RED | GREEN |
|---|---|---|---|
| 1 | `crates/sdax-tokio/tests/driver.rs`: `r00_the_plans_bodies_are_reachable_as_host_api` — `sdax::host::bodies_of(&plan)` yields a `BodySource` whose `export()` is `None` before anything ran | `error[E0432]: unresolved imports 'sdax::host::bodies_of', 'sdax::host::BodySource'` | `crates/sdax/src/host/bodies.rs` (new, 213 lines): `Bodies` moved out of `plan.rs` with the `#[allow(dead_code)]` dropped, plus `BodySource`, `Task`, `bodies_of`. Two gaps the move exposed and fixed: `PlanBuilder::component` recorded only the child's **IR**, so a component's inner nodes had no runnable bodies at all — `Bodies` now carries `children`; and a child scope's `import` node had no way to receive the ancestor's value, because `Deps::fetch` reads *this* plan's `Slots` — `import<T>` now records an `ErasedImport` copy where `T` is still known. `builder.rs`, `terminals.rs`, `plan.rs`, `host.rs` follow the move |
| 2 | same file, `r01_a_plan_runs_to_ok_on_the_adapter` — a resource, a step needing it and an export, started on a paused tokio runtime, ends `Ok` with `output == 7`, a clean report and no tracked task left | `error[E0599]: no method named 'start' found for struct 'sdax::Plan<Out, In>'` | `crates/sdax-tokio/src/{body,driver,running}.rs` (new): the run driver — a loop around `Machine::step` over an `mpsc` inbox, one task per attempt, one handle per node, timers as tasks; `PlanStart::start`/`start_with`, `Running` (`Future`, `ready`, `shutdown`, `cancel`, `snapshot`, `#[must_use]`, drop guard), `RunOptions`, `RunRecord`, `Snapshot`. Host-surface additions: `Observer::report` (default no-op, so `C-14`'s report can reach an observer nobody is awaiting), `Machine::{kind_of, deadline_for, nodes}`, `Stop::on` made public. `TokioRuntime::close_and_wait` folded into `shutdown` (Stage 0 open question 7), `TokioRuntime::with_clock` added, and `Drop for TokioRuntime` emits `RuntimeDroppedWithLiveRuns`. One new tokio feature, `sync`, justified in the manifest |
| 3 | `crates/sdax-tokio/tests/conformance.rs`: the whole of suite (c), **included by path** from `crates/sdax-testkit/tests/conformance/` and compiled against a second `Drv`, plus `conformance/differential.rs` — fourteen `(plan, script)` cases run on **both** drivers with the traces and the reports compared | first `error[E0432]: unresolved import 'sdax_testkit::Recorded'`, then **43 of 58 rows failed** with `rejected: Started for a node with no body in flight` | the driver announced `Event::Started` for *cleanup* bodies too, which the machine rightly refuses (D1) — `run_body` gained an `announce` flag and the release/compensate path passes `false`. `crates/sdax-testkit/src/scripted.rs` (new, 268 lines): `ScriptedBodies`, a `BodySource` that runs a `Script` on the engine clock, with `quiet_scripted_panics`. `Driven::from_recorded` + `Recorded` let another driver hand the same material to the same checker. Each conformance module's `use sdax_testkit::ScriptedDriver` became `use crate::Drv as ScriptedDriver`, one line per module, so all 65 call sites are untouched and the suite has one source (LBT-009). **58 rows + 3 differential tests green on the adapter**; the normalised traces are identical to the pure machine's |
| 4 | `crates/sdax-tokio/tests/driver.rs`: `C-14` (drop of a live `Running` drains and reports to the observer), `C-11` on the adapter, `R-02` (no release before the join of an aborted body), `R-03` (runtime shutdown; the dropped-runtime event), `R-04` (a declared pool's bound, measured by a high-water mark the blocking bodies keep themselves), `R-06` (a panic in a body and in a release), `R-07` (four concurrent runs of one plan value; a pool of **one** per run, so a shared pool would show a high-water mark of one and it shows four) | compile errors first (`no associated function named 'new_multi_thread'`, `cannot move out of 'rt' because it is borrowed`); then `R-04` spun for the full 2 s of its own deadline | `Running::ready` takes `&mut self` and launches the run if nothing has (waiting for the readiness of a run nobody ever polled would wait forever); `TraceRecorder` records `Observer::report` as a `ReportSummary` (a `Report` is not `Clone`); `rt-multi-thread` added as a **dev-dependency** feature only, with the reason in the manifest. `R-04`'s wait now also ends when every body has been in, so the last one waits for nobody: 2.0 s → 0.00 s |
| 5 | `crates/sdax-tokio/tests/conformance/multi_thread.rs`: `R-05` — eight programs on `new_multi_thread(2)` with a **compressed** clock (one engine second per 5 ms), invariants only | `i16 budget expiry: INV-8: 4.5579332s elapsed from Settling to End, over the budget 3s`, then, intermittently (1 run in 6), **`i07 restart: rejected: ServeEnded for a service that is not serving`** | INV-8 is not asserted under a compressed real clock and the module says why: a real timer fires at or after its deadline, and the compression multiplies the scheduler's jitter by the factor. It is asserted exactly under paused time. The second failure was a **real driver bug**: the serve future was spawned on the start body's `Ok` alone, so a start body that returned *after* the engine had signalled it — `Interrupted`, not ready (T5) — left a serve task nobody owned (INV-15) and fed the machine a `ServeEnded` for a node that was not serving (D1). The driver now holds the serve future and arms it only if the machine says the node became `Ready`, dropping it otherwise. Pinned under paused time by `r05_regression_a_signalled_start_body_never_starts_its_serve_future`, RED-checked (`left: 1, right: 0`) |
| 6 | `crates/sdax-tokio/tests/spike.rs`: `S-01` — transport with an injected failure, storage, routing over both, a heartbeat service; a walk with random cancellation, drops and panics, asserting no orphans (`tracked() == 0`), no held slot without a release (counted by the bodies), every fault reached in the report (the transport body's own record against the report) and the bounded shutdown (INV-8, exact under paused time) | three failures in the first 40 cases, all in the harness: `exactly one report: left 0, right 1` (a `Running` dropped before its first poll has started nothing, which is the laziness working); `INV-9 … the report does not say so` (the reconstructed report for a dropped run was empty); `a task outlived the run: left 1, right 0` (a spawned request task still sleeping) | the spike launches the run with `ready()` before dropping it; a `Captured` observer rebuilds a full `Report<()>` from `Observer::report` (node, order, phase and fault *label* are the original's, the payloads are re-boxed and it says so); the request is made from the awaiting future via `tokio::time::timeout(at, &mut running)` rather than a task, so `tracked()` counts the engine and not the harness. `quiet_scripted_panics` also learned that `panic!("{x}")` carries a `String`, not a `&'static str`. **300 cases green by default; 20 000 walked clean in 5.2 s** |
| 7 | `crates/sdax-tokio/tests/monte_carlo.rs`: the testkit's own generator, driven by the adapter — same seeds, same plans, same scripts, invariants only | at 20 000 cases, **case seed `5722278272308672243`**: `rejected: cancel after End — CancelRequested`, from a script with two requests at `t=0` | a real driver bug: the simulator stops feeding once `ended` and the driver did not. `Driver::handle` now returns early once `End` has been seen — a late request or a body outcome queued behind the `End` is dropped rather than fed to a machine that would rightly refuse it (D1). **120 000 cases walked clean in 37 s** |
| 8 | none — the finishing pass | `cargo clippy … -D warnings` on `whys: Vec<(Time, String, Vec<(String, Reason)>)>` (type complexity), on `Driver::new` (eight arguments) and on the `loop` in `Running::poll` that never loops; `RUSTDOCFLAGS="-D warnings"` on a stale `Self::close_and_wait` link | `WhyAt` made public in the testkit, a scoped `allow` with a reason on `Driver::new`, the loop flattened, the link retargeted. `crates/sdax/tests/surface.rs` extended: the eight Stage-1 script types the Stage 1 report listed as unpinned, and the Stage-2 host additions (`bodies_of`, `Bodies`, `BodySource`, `Task`, `Machine::{kind_of, deadline_for, nodes}`). `tests/driver.rs` reached 545 lines and was split into `driver.rs` (332) and `substrate.rs` (261). All seven gates green: **253 passed, 0 failed, 2 ignored** |
