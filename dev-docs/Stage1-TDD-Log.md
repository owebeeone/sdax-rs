# Stage 1 — TDD log

LBT-007: every behaviour change begins with a failing test. One line per step:
the test written, the RED evidence observed, and the change that made it GREEN.
RED lines quote what the compiler or the runner actually said.

Convention: a test that does not compile *is* a failing test. The Stage 1
test sources are `sdax-v1/B/CanonicalTests.md` § 3 (`P-16`, `P-17`), § 4
(suite (c), `C-*`) and § 5 (`S-02`), adapted to the adopted contract as the
Stage 1 brief says: `Mode` is explicit, `persistent()` exists, `spawns` is
declared, the report order is defined. Where B's row assumes a resources-only
plan stays up until `@t shutdown`, the plan is built `Mode::Resident`
(INV-19 allows it); where the row says *finite*, `Mode::Finite`.

Resume point: read the last row of the table below; the workspace is green at
every row unless the row says otherwise.

| # | test written first | RED | GREEN |
|---|---|---|---|
| 1 | `tests/machine.rs`: P-16 (a node with `needs (db, a, b)`, `exclusive(db)`, `limit(cpu)` is spawned only when every need is Ready **and** both grants are taken in the same step; with the pool full it is `Waiting` and `why` names the pool; a sibling holding the lock is named as `Exclusive`), P-17 (`FailFast`: on `NodeErr(B)` the effects are `Abort(A)`, `Abort(C)`, `Emit(Skipped(Down))`; no `Release` until both `NodeCancelled` arrive; then releases in graph order; `End(Failed)`; a late event is `Reject`ed), and a template plan is refused naming Stage 3 | `error[E0432]: unresolved imports 'crate::host::engine::Machine', 'crate::host::engine::RunState'`, `error[E0433]: cannot find type 'NodeState'`, `error[E0599]: no variant ... named 'Reject' found for enum 'engine::Effect'` | `host/engine/{table,state,admit,faults,settle,cleanup,machine}.rs` (the machine, T1–T8 for static plans and components), `sim/{script,simulator}.rs` (the data types the machine's schedule needs, and the stepping simulator). Vocabulary changes: `Event::NodeErr` carries `FaultKind`, `Event::ServeEnded` carries `Option<FaultKind>`, new `Effect::CancelTimer`, `Effect::Reject(Rejected)`. One test expectation was wrong, not the machine: `Db`'s dependents are `A` and `N`, so its release opens when `A`'s ends, whatever `B` is doing. 4 passed; workspace green |
| 2 | `crates/sdax-testkit/tests/conformance/{startup,faults,cancel,cleanup,retry,components}.rs` over `corpus.rs`: suite (c) rows C-01, C-02, C-04, C-05, C-07, C-08, C-08b, C-09, C-11, C-12, C-12b, C-15, C-16, C-18, C-20, C-21, C-23, C-24, C-24b, C-26, C-27, C-29 (+ variant), C-32, C-33, C-38 (+ variant), C-40, C-41, C-50, C-52, C-53, C-54, C-55, C-56, C-57, C-58, C-59, C-60, C-61 (+ variant), C-62, C-63, C-66, C-67, C-68, C-69, written against `ScriptedDriver::run`, `Driven::check`, `eol::Eol` and the trace checker | `error[E0432]: unresolved import 'sdax_testkit::ScriptedDriver'`, `'sdax_testkit::eol'` (batch 1); then real failures, one per batch, each fixed in the machine or the sim, never by bending the row: **C-02** `Script` kept the first entry for a node (`left: 2.0, right: 11.0`) → a later entry replaces the earlier; **C-21** `left: ["B"], right: ["B", "C"]` — the "after the cancel decision everything is Interrupted" rule lost a fault that was already due when the abort was issued → after `Abort` the body's own outcome stands (the abort lands between polls), after `Signal` the return is the answer to the cancel (C-60); **C-26** checker `INV-10: Conn attempt 1 was interrupted and is reported as a fault` → an interrupt during backoff is the *next* attempt being cut short, so `Interrupted` carries attempt k+1; **C-40/C-57** checker `INV-9: Renew attempt 1 failed (serve, Error) and the report does not say so` — the restart never started: a `Steady` scope never admitted again → `admit` re-enters `Admitting` and `check_steady` returns it | `sdax-testkit/src/{driver,eol}.rs`, `invariants/trace.rs` (INV-1…5, 7…12, 15, 18, 20, `orphans: none`, report ⊆ trace ⊆ report, F4 order), `TraceKind::Settling` (the observable start of the budget, needed by INV-8), a try-step's scripted `Err` delivered as `NodeOk` (OD-5). 44 passed; workspace green |
| 3 | the `mc` module's own unit tests (`prng.rs`: `same_seed_same_sequence` pins splitmix64's first word for seed 0, `helpers_stay_in_range` fuzzes `below`/`range`/`f64`/`subset`/`permutation`) — a module that does not compile is a failing test, and `mc` had never been compiled: `mod mc;` was missing from `lib.rs` | `error[E0106]: missing lifetime specifier ... common<D: Deps, K>(mut n: Node<'_, D, K>, a: &Attrs) -> Node<'_, D, K>`; then clippy `-D warnings`: `if_same_then_else` (`if kind == Kind::Join { 2 } else { 2 }`), `unnecessary_literal_unwrap` (`Shutdown::within(budget.unwrap())` on a literal `Some`), `unused_variables: kind` | `pub mod mc;` in `lib.rs` and a new `mc/mod.rs`; `common` given a named lifetime `'p` tied to the builder, not the attributes; `gen.rs` (551 lines, over the ~500 guideline) split by responsibility into `keys.rs` (the `Unit` value, the `Keys`/`Needs`/`Attrs` bookkeeping and the author-surface calls, 211 lines), `mutate.rs` (the catalogue of deliberate declaration mistakes, 52) and `gen.rs` (the node generator itself, 454); `keys`/`mutate` kept private to `mc`. 157 passed; workspace green |
| 4 | `crates/sdax-testkit/tests/monte_carlo.rs`: the case loop (a seed → a plan → a script → the same `ScriptedDriver` the conformance suite uses → `invariants::check_trace`), the invalid-plan arm (`expect_invalid` must be refused by that `Rule`), INV-14 (the same case seed re-runs to a byte-identical trace), `a_run_seed_reproduces_the_whole_walk`, and the coverage histogram with a floor per corner | run seed `6746427589533237249`. Case 4 `V-UNUSED-POOL: pool "pool2" has no user` on a plan with no mutation; case 6 `rejected: Held for a node whose body is not in flight`; case 18 `INV-9: B0 attempt 1 failed (run, Error) and the report does not say so` with `End(Ok)`; case 84 `INV-15: C1 attempt 1 started and never ended (orphan)`; case 93 `INV-20: faults: C4/C4/R0 has record steps [4, 2], declaration says [4, 0]` | four fixes, three of them in the core: **(gen)** `pool_of` used a random pool after making a new one, so a fresh pool could end up with no user — a made pool is now used; **(sim)** a `Body` whose `held` is later than its own ending fed the machine a `Held` for a body that had already returned — the hold is clamped to the ending instant and delivered first, since a body registers its value before it returns (`simulator.rs`, documented on `Body::held`); **(machine)** `cleanup::abandon` dropped the node's accumulated faults, so a node abandoned mid-retry lost its earlier `Fail` and the run ended `Ok` — it now flushes them like `node_failed` and `interrupted` do; **(machine)** `settle::interrupt` settled a component's inner scope but emitted no terminal observation for the component itself, so its trace went `Start(Prepare)` → `ReleaseStart` — it now emits `Interrupted { held: started }`; **(machine)** `table::flatten` numbered `RecordOrder.steps` with the builder's `key.idx`, which counts `import` nodes that `PlanView` does not show, so F4 order and view order disagreed in any plan with a component that imports — the step is now the position among the nodes the view shows. Conformance still 44 passed; MC walk still failing further in |
| 5 | the same MC walk, deeper (cases 143, 147, 422, 456, 1095, 1140 of run seed `6746427589533237249`) | `INV-9: B1 attempt 1 failed (run, Error) and the report does not say so`; `the mutation unconsumed try_step should have been refused by TryUnconsumed, and built instead`; `INV-9: S0 attempt 1 failed (serve, Error) and the report does not say so`; `INV-5: R0's cleanup started at #6 while dependent E1 had not finished`; `INV-5: C0's cleanup started at #11 while dependent B2 had not finished`; `T5: C2/C2/Consume0 started at #12 after the run settled` | **(machine)** `settle`'s `Pending`/`Waiting` arm dropped a node's earlier faults when the scope settled while it waited for its *next* attempt (a zero backoff, or a grant it never got) — it now flushes them like the `Backoff` arm; **(gen)** `pick_needs` never marked the try-step it consumed, so `Mutation::TryUnconsumed` fired on already-consumed try-steps and the plan built; **(machine)** a service's serve fault was emitted to the trace and then *dropped* when a restart followed — it is now parked on the node, cleared by a `Ready` that follows and flushed if the restart never gets there; **(checker)** `cleanup_ended_by` demanded a compensation for an ambiguous `on_ambiguous(Compensate)` effect even when it is `persistent()` and has none — the core's `compensates_ambiguity` excludes persistent, the checker now does too (see the report's open questions: the validator accepts that pair); **(machine)** `cleanup::abandon_all` walked one declaration-order pass, so a component's inner `ReleaseStart` could precede the `Abandoned` of a node depending on it — two passes now, bodies first in declaration order, inner release graphs after in reverse; **(machine)** `component_faulted` left the inner scope `Admitting` under an inner `Isolate`, so a sibling that became ready afterwards started a body after the run settled — the failed component's inner scope is now settled. Conformance still 44 passed |
| 6 | the walk carried on: cases 1708, 11, 1946, 2455, 545, 2258 of the same run seed, plus a `stuck` run promoted to a failure (a run that stops short of `End` gets none of the end-time checks, so it must not be tolerated) | `INV-9: E1 attempt 1 failed (prepare, Panic) and the report does not say so`; then, after recording that panic, `INV-10: S0 attempt 1 was interrupted and is reported as a fault` — the two invariants in direct contradiction; case 1946 did not fail but **hung**, 100 % CPU, `attempt 249` at `t=1237s`; `the mutation duplicate name should have been refused by DupName, and built instead`; `stuck: the run stopped short of End` at 545 and again at 2258 | **(checker, after reverting a wrong machine fix)** INV-10 is the contract's answer: a body the engine cancelled is not reported, and a panic is observed in the trace only. INV-9's escape knew `Interrupted` but not `Ambiguous`, which is that same terminal observation for an effect that had not held (INV-11) — the escape now names both, and the machine is untouched; **(driver)** the liveness guard sat *after* the quadratic prefix check and at 100 000 steps, so a non-terminating run ground instead of reporting — it is checked first and at 1 000 steps, which no honest run approaches; **(script gen)** a resident plan with `Restart::on_error` and no `max` never ends by itself, correctly, so the generator now always appends a backstop `Request::Shutdown` at 20–25 s, well past the 0–15.5 s window the random requests are drawn from; **(gen)** `Mutation::DupName` took its name from `lines[0]`, which can belong to a *child* plan, where repeating it is not a duplicate — it now reuses a name declared in this scope; **(machine)** `settle` and `skip_dependents` skipped a component without touching its inner scope, whose `Pending` nodes then held the release gate of everything they import shut — new `skip_scope` marks a never-started inner scope `Ended` and its nodes `Skipped`, recursively; **(machine)** a *ready* component's inner scope was never settled by its parent settling, so `in_flight` counted the component while the component waited for the run to reach cleanup — a deadlock on any plan with a `Mode::Resident` component and an unbounded shutdown; `settle` now settles a live inner scope whatever the component's own state. 3000-case walk green, every floor cleared, no stuck runs; workspace 157+2 tests pass in ~1.8 s |
| 7 | `crates/sdax-testkit/tests/conformance/regressions.rs` (new module of suite (c)) — one named test per bug the walk found, written by hand in the shape that produced it, each carrying its MC case seed; and `every_seed_that_found_a_bug_replays_clean` in `tests/monte_carlo.rs`, which replays every recorded case seed | each test written before its fix was in place would have failed on the assertion it names; written after the fixes, the RED evidence for each is the MC failure quoted in rows 4–6. Two wrote themselves wrong first: `abandoned("B")` was `None` because the shutdown at `1.0` raced the fault at `1.0` (moved to `1.5`), and `is_interrupted`/`is_cleanup_start` needed importing by name | 7 hand-written regressions + the seed replay. Also in this round, from the 50 000-case run seeded `20260906`: **(machine)** `skip_dependents` dropped the parked faults exactly as `settle` did (case 1424, seed 13281131733918634243); **(machine)** an inner scope whose *own* budget expired went straight to `Cleanup`, so the component's `ReleaseOk` had no `ReleaseStart` and its attempt was an orphan — the transition now belongs to `open_component`, which the gate guards, and `sweep` opens it when the gate allows (case 3018, seed 3320581108311238895); **(gen)** `Mutation::PoolStarve` put the starving step on `pools.last()`, which is whatever `pool_of` made most recently, so nothing was starved and the plan built (case 4569, seed 6593995201653377501). 51 conformance + 3 MC tests; workspace green |
| 8 | `tests/conformance/regressions.rs::mc_the_budget_does_not_open_an_inner_release_before_a_dependent_component_ends` — two components, `C4` importing `C2`'s export, `C4`'s inner release started at the shutdown and hanging, the root budget expiring on it; written by hand from MC case index 11489 of run seed `20260906` (case seed `2796147909731581100`) | the hand-written test: `INV-5: C2's cleanup started at #14 while dependent C4 had not finished`, trace `#14 C2 ReleaseStart` before `#16 C4 ReleaseOk` — the same message the walk reported at case 11489 | **(machine)** `cleanup::abandon_inner` emitted the component's `ReleaseStart` and forced `Settling → Cleanup` unconditionally. A dependent *component* abandoned in the same instant only reaches `ReleaseOk` in the sweep that follows, so no ordering of `abandon_all`'s two passes can bring it forward — the transition now belongs to `open_component`, which the gate guards, and `sweep`/`try_cleanup` open it as soon as the gate allows. Exactly the shape of the `on_budget_timer` fix in row 7. Seed added to `SEEDS_THAT_FOUND_BUGS`. 55 conformance + 3 MC tests; workspace green |
| 9 | `tests/conformance/regressions.rs::mc_a_component_whose_inner_scope_settles_by_itself_ends_its_own_attempt` — a component whose child plan holds a `terminal` service and a resource that never returns, so the inner scope settles under a parent that is still admitting; hand-written from MC case index 33699 of run seed `20260906` (case seed `2682709018262434330`), which the walk could only reach once row 8's bug was fixed | the hand-written test: `INV-15: C attempt 1 started and never ended (orphan)`, trace `#0 C Start(Prepare)` → `#6 C ReleaseStart` with nothing between — the same message the walk reported at case 33699. Checked against the machine *before* row 8's fix as well: the walk still stops at 11489, so this bug was masked, not caused | **(machine)** `settle::interrupt` ends a component's attempt when the *parent* settles, but an inner scope that settles for a reason of its own (a `terminal` service finishing inside it, `Cause::Terminal`) left the component `St::Running` for ever. `cleanup::open_component` now ends a still-`Running` component's attempt with `Interrupted { held: started }` before it emits `ReleaseStart` — the same terminal observation and the same reason as `settle::interrupt`, at the one point every route into a component's release passes through. Seed added to `SEEDS_THAT_FOUND_BUGS`. 56 conformance + 3 MC tests; workspace green |
| 10 | `tests/conformance/regressions.rs::mc_a_waiter_a_nested_admit_already_started_is_not_started_twice` — a component whose child plan `import`s an outer key (so the component and a sibling resource queue in the same admit pass) and holds a `join` with no needs (so the inner graph comes up synchronously inside `start`); hand-written from case index 66182 of run seed `20260906` (case seed `2300709931059428012`), which a **200 000**-case walk found beyond the 50 000 the brief asked for | the hand-written test: `INV-12: R attempt 2 started while attempt 1 was in flight`, `INV-15: R attempt 1 started and never ended (orphan)`, `rejected: Held for a node whose body is not in flight`, trace `#6 R #1 Start(Prepare)` immediately followed by `#7 R #2 Start(Prepare)` — the same three messages the walk reported at case 66182. The first attempt was wrong, not the machine: with a child plan that imports nothing the component needs nothing and starts before the sibling is queued, so it passed; the import is what puts them in one pass | **(machine)** `admit`'s grant loop iterated a `waiters` list taken before the first `start`. `start` on a component admits the inner scope, and an inner scope that reaches steady synchronously makes the component `Ready`, which re-enters `admit` for the *same* scope; that nested pass starts the later waiters, and the outer loop then started one of them again. The loop now re-reads the slot before granting and skips any waiter that is no longer `St::Waiting`. Seed added to `SEEDS_THAT_FOUND_BUGS`. 57 conformance + 3 MC tests; workspace green |
| 11 | `tests/conformance/regressions.rs::mc_a_timed_out_blocking_attempt_does_not_end_the_next_one` — a `blocking_step` with `within(2s)` and `retry(attempts(3))` whose attempt 1 thread ends at `@t 4s`, two seconds after the deadline that already failed it; hand-written from case index 168112 of run seed `20260906` (case seed `11779147375297488456`), found by the same 200 000-case walk | the hand-written test: `left: 3, right: 2` on `attempts("B")`, trace `#1 Fail(Run, Timeout)` at t=2, then `#2 Fail(Run, Error)` at t=4 — attempt 1's late thread ending credited to attempt 2 — and `#3 Start(Run)`/`#3 Ready` in the same instant. In the walk this escalated to `rejected: Started for a node with no body in flight`, because the third attempt reached `Ready` before its own `Started` was delivered | **(sim)** `on_within_timer` says in as many words that a blocking body cannot be aborted (T7) and "the thread's later outcome is ignored" — but nothing ignored it: `Event::NodeOk`/`NodeErr` name a node, not an attempt, so the machine credited the stale outcome to whichever attempt was in flight. A run driver holds one task handle per node and drops a superseded attempt's result; `sim::Simulator` stands in for one, so `Effect::Spawn`/`SpawnBlocking` now drops that node's queued `Started`/`Held`/`NodeOk`/`NodeErr`/`NodeCancelled` before queueing the new attempt's. Cleanup outcomes cannot be pending there (INV-12) and serve episodes are not attempts, so both are left alone. Seed added to `SEEDS_THAT_FOUND_BUGS`. 58 conformance + 3 MC tests; workspace green |
| 12 | *(no behaviour change — paperwork)* re-capture and report: the default coverage histogram after every fix above, the timings, LOC, the suite-(c) audit, `dev-docs/Stage1Report.md`, `README.md` § Status, the contract's § 11 stage table and § 5/§ 7/§ 9/§ 10/§ 12 stage annotations, and three new rows in the contract's Decisions table (`OD-PANIC-CANCELLED`, `OD-PERSIST-AMBIG`, `OD-BACKSTOP`, all 2026-09-06) | n/a — nothing behavioural was written, so there is nothing to see fail. What was *measured* rather than asserted: every coverage floor still clears on its own at the default 3 000 cases (thinnest is `exclusive contention`, 8 against a floor of 5), so no floor moved; the fast-loop budget in `AGENTS.md` (~0.55 s / ~3.8 s) is stale against the measured ~1.65 s / ~8.5 s, and is reported as a finding rather than edited; and the brief's suite-(c) audit undercounted the omissions — `C-14` is a fifth, deferred to **Stage 2** (drop of `Running`, the drainer) and already recorded in the module doc of `tests/conformance/cancel.rs` | `dev-docs/Stage1Report.md` (526 lines): what Stage 1 contains, all seven gates verbatim, the timings with the commands and the machine, LOC per crate with the counting command, the re-captured histogram at 3 000 / 50 000 / 200 000 cases, all 24 defects the walk found with their 21 pinned seeds, the 41-of-46 suite-(c) audit with a reason per omission, `S-02` recorded as **not run**, five deviations, five open questions, and the `Effect`/`Event` table Stage 2's tokio driver must implement (plus the one blocker: `plan::Bodies` is `pub(crate)` and must move under `sdax::host`). 174 passed, 2 ignored; all seven gates green |
